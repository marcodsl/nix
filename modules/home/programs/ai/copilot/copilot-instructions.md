# Copilot instruction index

Use this file as the single source of truth for Copilot's additional local instructions.

Before using, reading from, or writing to the memory MCP server, read `./instructions/memory-server.md` in this same directory to understand how memory should be managed.

Keep detailed memory guidance in `./instructions/memory-server.md` and reference deeper instruction files from this document instead of duplicating them inline.
